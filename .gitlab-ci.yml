image: eclipse-temurin:17

stages:
  - build
  - test

cache:
  paths:
    - .m2/repository

build:
  stage: build
  script:
    - cd Project/Spring
    - mvn clean compile

test:
  stage: test
  script:
    - cd Project/Spring
    - mvn test
